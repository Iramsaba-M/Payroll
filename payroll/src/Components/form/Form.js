import React from 'react'
import FormComponent from './FormComponent';


const Form = ({configs}) => {

  return (
    <div>
        <FormComponent config={configs} />
    </div>
  )
  }

export default Form;

